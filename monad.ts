(function() {

    class Thing {
        constructor(x) {
            return {
                value: x
            }
        }
    }

    // const addone = (x: number): Thing => x + 1

    function addone(x) : number {
        return "dfji";
    }

    // function addone(x: number):Thing {
    //     // return new Thing(x + 1);
    //     return "djfio";
    // }

    // console.log(
    //     addone(1)
    // );

})()
